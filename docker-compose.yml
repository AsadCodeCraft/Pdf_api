services:
  html-to-pdf-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: html-to-pdf-api
    ports:
      - "3020:3020"
    env_file:
      - .env
    environment:
      PORT: 3020
      NODE_ENV: production
    restart: unless-stopped
